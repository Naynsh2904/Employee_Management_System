import React from 'react'

function AuthContext({children}) {

  // useEffect(() => {
  // SetLocalStorage()
  //   GetLocalStorage()
  // },)
  return (
    <div>{children}</div>
  )
}

export default AuthContext